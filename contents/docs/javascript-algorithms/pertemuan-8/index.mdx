## 📌 Pertemuan 8: Asynchronous JavaScript (Callback & Promise)

Pemrograman async memungkinkan kita menangani proses yang memerlukan waktu seperti loading data dari server.

### 📞 Callback Function

```js
function greet(name, callback) {
  console.log("Hello " + name);
  callback();
}

greet("Andi", () => console.log("Finished"));
```

### 💎 Promise

```js
const promise = new Promise((resolve, reject) => {
  setTimeout(() => resolve("Success!"), 1000);
});

promise.then(console.log).catch(console.error);
```

---
