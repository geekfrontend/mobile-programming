## 📌 Pertemuan 6: Object & Manipulasi Data

Object adalah struktur data yang menyimpan data dalam bentuk pasangan key-value (kunci dan nilai).

### 📚 Deklarasi dan Akses Object

```js
const student = {
  name: "Ani",
  age: 17,
  major: "Science",
};

console.log(student.name); // Ani
console.log(student["age"]); // 17
```

### 🛠️ Operasi Dasar pada Object

```js
student.class = "XII"; // Menambah properti
student.age = 18; // Mengubah properti
delete student.major; // Menghapus properti
```

### 🔄 Iterasi pada Object

```js
for (let key in student) {
  console.log(key, student[key]);
}
```

<Note title="Latihan">
  Buat object `book` dengan property `title`, `author`, dan `year`, lalu
  tampilkan isinya menggunakan `console.log`.
</Note>

---
